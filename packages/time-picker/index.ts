import TimePicker from './TimePicker.vue';
import type { TimePickerEmits, TimePickerProps, TimePickerSize, TimePickerTheme, TimePickerExpose } from './types';

export const AleTimePicker = TimePicker;
export type { TimePickerProps, TimePickerEmits, TimePickerSize, TimePickerTheme, TimePickerExpose };
export default TimePicker;
