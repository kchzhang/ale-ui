import Tag from './Tag.vue';
import type { TagEmits, TagProps, TagSize, TagType } from './types';

export const AleTag = Tag;
export type { TagProps, TagEmits, TagSize, TagType };
export default Tag;
