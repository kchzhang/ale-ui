import Badge from './Badge.vue';
import type { BadgeProps, BadgeSize, BadgeType } from './types';

export const AleBadge = Badge;
export type { BadgeProps, BadgeSize, BadgeType };
export default Badge;
