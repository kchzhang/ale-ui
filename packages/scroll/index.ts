import Scroll from './Scroll.vue';
import type {
  ScrollProps,
  ScrollEmits,
  ScrollExpose,
  ScrollTheme,
  ScrollSize,
  ScrollDirection,
  ScrollBehavior,
  ScrollPosition,
  ScrollSizeInfo,
  ScrollState
} from './types';

export const AleScroll = Scroll;
export type {
  ScrollProps,
  ScrollEmits,
  ScrollExpose,
  ScrollTheme,
  ScrollSize,
  ScrollDirection,
  ScrollBehavior,
  ScrollPosition,
  ScrollSizeInfo,
  ScrollState
};
export default Scroll;
